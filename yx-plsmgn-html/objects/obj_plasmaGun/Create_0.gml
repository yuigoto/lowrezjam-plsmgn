/// @description Bootstrap gun data

// Set shot point offset
off_x = 5;
off_y = -1;

// Set direction, distance and modifier
off_dirs = point_direction(0, 0, off_x, off_y);
off_dist = point_distance(0, 0, off_x, off_y);
off_mods = 1;
